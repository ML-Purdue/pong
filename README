This is the pong project for SigART

To build the project, run:
$ make

To clean the respository:
$ make clean

Check the wiki or google docs document for up-to-date information about this project.
 
SVN Commands:
svn stat: checks for any modifications to the repository.

svn up: checks for changes and update files locally.

svn add: adds an unversioned file to the repository.

svn commit: send local changes to the remote repository.

svn checkout: Run once at the beginning to get the repository on your local machine.
